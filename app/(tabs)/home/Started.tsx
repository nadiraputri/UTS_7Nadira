import { View, Text } from 'react-native';
import React from 'react';

const settings = () => {
    return (
        <View>
            <Text>Started</Text>
        </View>
    );
};

export default settings;